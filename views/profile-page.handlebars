    <div class="mb-5 pt-3 profile-head">
        <a href="/profile-page/create/"><button id="create-post" class="button is-primary uk-button uk-button-default">New Post</button></a>
        
        <h1 class="title pb-4">My Posts</h1><hr>
{{#if posts.length}}
    </div>

    {{#each posts as |post|}}
        {{> post-info post}}
        
            {{!-- <a href="/dashboard/edit/{{post.id}}"><button class="button mb-5 ml-5 is-warning">Edit Post</button></a> --}}
        </div>
    {{/each}}

{{else}}
<h6 class="pb-4 has-text-centered is-italic">No Posts</h6>

{{/if}}